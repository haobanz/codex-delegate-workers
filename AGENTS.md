# 项目规范

## 临时文件

- 主代理和所有子代理产生的临时文件、草稿、日志、截图、下载和测试数据，统一放在项目根目录的 `tmp/`；不存在时先创建。
- 从子目录工作时仍使用项目根的 `tmp/`。临时命令显式指定目录；需要时仅为该命令设置 `TMPDIR`、`TMP`、`TEMP`，不要使用系统临时目录（如 `/tmp`、`/var/tmp`、系统 `%TEMP%`）。
- 在 `tmp/` 下使用能识别用途的独立子目录，委派时传递该绝对路径；完成后只清理本次创建的内容，保留其他任务文件。
- `tmp/` 不提交到 Git。目录无法创建或不可写时明确报错，不回退到系统临时目录。
